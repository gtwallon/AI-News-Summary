# AI-News-Summary

This notebook contains an AI tool that pulls news articles from the NewsAPI according to a search query, further filters the articles by keyword, uses an OpenAI LLM to rank the articles by their relevance to a specified topic, then uses an OpenAI LLM to summarize the most relvant articles in a specified number of sentences. 
